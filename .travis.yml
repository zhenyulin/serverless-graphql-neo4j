---
language: node_js
cache:
  yarn: true
  directories:
    - node_modules
install:
  - make install
before_script:
  - make test-setup & sleep 5
script:
  - make test-coverage
after_success:
  - cat coverage/lcov.info | coveralls
  - make deploy
branches:
  only:
    - master
